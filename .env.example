# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Site URL for authentication redirects
# This should be set to your production domain in production environment
# For development, you can leave this empty or set to http://localhost:3000
# The system will automatically detect localhost and use the correct URL

# For Production (Vercel):
NEXT_PUBLIC_SITE_URL=https://full-system-20.vercel.app

# For Development (comment out or leave empty):
# NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Note: 
# - In development, the system automatically detects localhost and uses http://localhost:3000
# - In production, set this to your actual domain
# - The redirect URL will automatically append '/auth/callback'